# MongoDB 配置
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password
MONGO_DATABASE=todo-app

# API 配置
API_PORT=5000
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRE=30d
NODE_ENV=production

# 前端配置
FRONTEND_PORT=80
